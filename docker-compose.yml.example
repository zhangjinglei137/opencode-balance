services:
  app:
    image: thntime/opencode-balance:master-new-api
    ports:
      - "${PORT:-3456}:${PORT:-3456}"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
